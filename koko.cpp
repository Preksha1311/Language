#include<bits/stdc++.h>
#define ll long long int
using namespace std;

int main() {
    freopen("input.txt", "r" , stdin);
    freopen("output.txt", "w" , stdout);

    vector<ll> c;
    for (int i = 0; i < 10; i++) {
        cin >> c[i];
        
    }
    cout<<"hi";
    return 0;

}