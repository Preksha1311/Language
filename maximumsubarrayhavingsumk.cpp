#include<bits/stdc++.h>
#define ll long long int
using namespace std;


int main() {
    freopen("input.txt", "r" , stdin);
    freopen("output.txt", "w" , stdout);
    ;


    ll x; cin >> x;
    vector<ll> arr;
    for (ll i = 1; i <= x; i++)
    {
        cin >> arr[i];
    }

    vector<ll> pref;
    for (ll i = 0; i < x; i++) {
        pref
    }

    return 0;
}