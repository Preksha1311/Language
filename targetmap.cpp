#include<bits/stdc++.h>
#define ll long long int
using namespace std;

ll countSubarray_having_sumk(vector<ll> &arr, ll target) {
    ll n = arr.size();
    ll prefsum = 0, ans = 0;
    vector<ll> prefarr
// for(int i=0;i<n;i++){
// prefsum+=arr[i];
//}
    for (in ti = 0; i < n; i++) 
    }

int main() {
    freopen("input.txt", "r" , stdin);
    freopen("output.txt", "w" , stdout);

    ll s;
    vector<ll> arr(s, 0);

    for (int i = 0; i <= s; i++) cin >> arr[i];

    vector <ll> pref(s + 1, 0);]

    ll target; cin >> target;

    cout <<
         // for (int i = 0; i <= s; i++) {
         //     ll prefsum = 0; ll l; ll r;

         //     pref[i] += arr[i];
         //     cout << pref[i] << " ";

         // }



         return 0;
}